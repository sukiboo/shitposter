prompt:
  prompt: "a cat wearing a business suit"

image:
  provider: placeholder  # or "openai" (defaults: gpt-image-1-mini, 1024x1024, medium quality)

caption:
  provider: placeholder  # or "openai" (default: gpt-4o-mini)

publish:
  platform: telegram
