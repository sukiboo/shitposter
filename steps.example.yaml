steps:
  prompt:
    type: set_prompt
    provider: placeholder  # returns "Placeholder text"

  image:
    type: generate_image
    provider: placeholder  # or "openai" (defaults: gpt-image-1-mini, 1024x1024, medium quality)

  caption:
    type: generate_caption
    provider: placeholder  # or "openai" (default: gpt-5-nano, temperature=1.0)

  publish:
    type: publish_post
    platforms:
      - placeholder  # or "telegram"
